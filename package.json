{
    "name": "artgallery-web",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "workspaces": [
        "web",
        "server"
    ],
    "scripts": {
        "test-cli": "echo a & echo b",
        "dev": "start \"\" npm run server-dev && start \"\" npm run web-dev",
        "web-dev": "npm run dev --workspace=web",
        "server-dev": "npm run dev --workspace=server",
        "package": "npm run package --workspace=server -q",
        "build": "npm run build --workspace=web -q",
        "export": "node .github/workflows/genDistPkg.js",
        "format-images": "node resources/scripts/images.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/Peletic/artgallery-web.git"
    },
    "private": true,
    "dependencies": {
        "@stripe/react-stripe-js": "^5.2.0",
        "@stripe/stripe-js": "^8.1.0",
        "adm-zip": "^0.5.16",
        "firebase": "^12.3.0",
        "sharp": "^0.34.4"
    }
}
