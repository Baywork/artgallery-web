{
    "name": "artgallery-web",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "workspaces": [
        "web",
        "server"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=web",
        "server-dev": "npm run dev --workspace=server",
        "package": "npm run package --workspace=server -q",
        "build": "npm run build --workspace=web -q",
        "export": "node .github/workflows/genDistPkg.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/Peletic/artgallery-web.git"
    },
    "private": true,
    "dependencies": {
        "@stripe/react-stripe-js": "^5.2.0",
        "@stripe/stripe-js": "^8.1.0",
        "adm-zip": "^0.5.16",
        "firebase": "^12.3.0"
    }
}
